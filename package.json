{
  "name": "giftitto-qa-challenge",
  "version": "1.0.0",
  "private": true,
  "description": "Automatización E2E mínima (Playwright) para el flujo de Login de Giftitto QA",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
